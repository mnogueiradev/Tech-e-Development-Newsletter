{
  "name": "testando",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd newsletter-frontend && npm install && npm run build",
    "build:prod": "cd newsletter-frontend && npm install && npm run build",
    "start": "node server.js",
    "start:pm2": "pm2 start deploy/ecosystem.config.cjs",
    "deploy:oracle": "npm install --omit=dev && npm run build && pm2 restart newsletter || pm2 start deploy/ecosystem.config.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "franc": "^6.2.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.6",
    "resend": "^6.12.2",
    "rss-parser": "^3.13.0",
    "sqlite3": "^6.0.1"
  }
}
